c({"name": "jQuery.event.special.dropout", "type": "attribute", "parents": ["jQuery.event.drop", "jQuery.event.special"], "comment": "<p><code>dropout</code> is called when a drag is moved out of this drop element.\nThe event handler gets an instance of [jQuery.Drag] passed as the second and a\n[jQuery.Drop] as the third parameter.</p>\n\n<pre><code> $('.droparea').on('dropover', function(ev, drop, drag) {\n     // Remove the drop element highlight\n     $(this).removeClass('highlight');\n });\n</code></pre>", "src": "jquery/event/drop/drop.js", "line": 36, "children": []})