c({"name": "jQuery.event.hover.hoverinit", "type": "attribute", "parents": ["jQuery.event.hover"], "comment": "<p><code>hoverinit</code> is called when a hover is about to start (on <code>mouseenter</code>). Listen for <code>hoverinit</code> events to configure\n[jQuery.Hover.prototype.delay] and [jQuery.Hover.prototype.distance]\nfor this specific event:</p>\n\n<pre><code> $(\".option\").on(\"hoverinit\", function(ev, hover){\n     //set the distance to 10px\n     hover.distance(10)\n     //set the delay to 200ms\n     hover.delay(10)\n })\n</code></pre>", "src": "jquery/event/hover/hover.js", "line": 168, "children": []})