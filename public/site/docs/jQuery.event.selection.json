c({"name": "jQuery.event.selection", "type": "page", "comment": "<p><a href=\"http://donejs.com/docs.html#!jQuery.selection\">jQuery.selection</a> adds <code>$.fn.selection</code> to set or retrieve the currently selected text range. It works on all elements:</p>\n\n<pre><code>&lt;div id=\"text\"&gt;This is some text&lt;/div&gt;\n</code></pre>\n\n<p>Can be used like this:</p>\n\n<pre><code>$('#text').selection(8, 12);\n$('#text').selection() // -&gt; { start : 8, end : 12 }\n$('#text').text().substring(selection.start, selection.end) // -&gt; some\n</code></pre>", "parents": ["jquerypp"], "src": "jquery/event/selection/selection.md", "children": []})