c({"name": "jQuery.event.special.draginit", "type": "attribute", "parents": ["jQuery.event.drag", "jQuery.event.special"], "comment": "<p><code>draginit</code> is triggered when the drag motion starts. Use it to customize the drag behavior\nusing the [jQuery.Drag] instance passed as the second parameter:</p>\n\n<pre><code>$(\".draggable\").on('draginit', function(ev, drag) {\n  // Only allow vertical drags\n  drag.vertical();\n  // Create a draggable copy of the element\n  drag.ghost();\n});\n</code></pre>", "src": "jquery/event/drag/drag.js", "line": 640, "children": []})