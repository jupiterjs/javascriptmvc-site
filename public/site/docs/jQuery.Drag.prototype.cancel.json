c({"name": "jQuery.Drag.prototype.cancel", "params": {}, "ret": {"type": "undefined", "description": ""}, "type": "function", "parents": ["jQuery.Drag.prototype"], "comment": "<p><code>drag.cancel()</code> stops a drag motion from from running.  This also stops any other events from firing, meaning\nthat \"dragend\" will not be called.</p>\n\n<pre><code>$(\"#todos\").on(\".handle\", \"draginit\", function( ev, drag ) {\n  if(drag.movingElement.hasClass(\"evil\")){\n    drag.cancel();    \n  }\n})\n</code></pre>", "src": "jquery/event/drag/drag.js", "line": 426, "children": []})