c({"name": "can.Model.static.id", "type": "attribute", "parents": ["can.Model.static"], "comment": "<p>The name of the id field.  Defaults to 'id'. Change this if it is something different.</p>\n\n<p>For example, it's common in .NET to use Id.  Your model might look like:</p>\n\n<pre><code>Friend = can.Model({\n  id: \"Id\"\n},{});\n</code></pre>", "src": "can/model/model.js", "line": 789, "children": []})