c({"name": "jQuery.Drop", "type": "class", "comment": "<p>The <code>$.Drop</code> constructor is never called directly but an instance of <code>$.Drag</code> is passed as the second argument\nto the <code>dropinit</code>, <code>dropover</code>, <code>dropmove</code>, <code>dropon</code>, and <code>dropend</code> event handlers. The third argument will be\nan instance of [jQuery.Drag]:</p>\n\n<pre><code> $('#dropper').on('dropover', function(el, drop, drag) {\n     // drop -&gt; $.Drop\n     // drag -&gt; $.Drag\n });\n</code></pre>", "parents": ["jQuery.event.drop"], "plugin": "jquery/event/drop", "download": "http://jmvcsite.heroku.com/pluginify?plugins[]=jquery/event/drop/drop.js", "test": "jquery/event/drag/qunit.html", "src": "jquery/event/drop/drop.js", "line": 52, "children": ["jQuery.Drop.static", "jQuery.Drop.prototype"]})