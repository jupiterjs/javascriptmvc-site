#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../docco/bin/docco" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../docco/bin/docco" "$@"
  ret=$?
fi
exit $ret
