C({"name": "jQuery.Model.static.attributes", "type": "attribute", "parents": ["jQuery.Model.static"], "comment": "<p>Attributes contains a list of properties and their types\nfor this model.  You can use this in conjunction with \n[jQuery.Model.static.convert] to provide automatic \n[jquery.model.typeconversion type conversion].  </p>\n\n<p>The following converts dueDates to JavaScript dates:</p>\n\n<pre><code class='javascript'>$.Model(\"Contact\",{\n  attributes : { \n    birthday : 'date'\n  },\n  convert : {\n    date : function(raw){\n      if(typeof raw == 'string'){\n        var matches = raw.match(/(\\d+)-(\\d+)-(\\d+)/)\n        return new Date( matches[1], \n                 (+matches[2])-1, \n                matches[3] )\n      }else if(raw instanceof Date){\n          return raw;\n      }\n    }\n  }\n},{})</code></pre>     \n\n<p>Attributes contains a list of properties and their types\nfor this model.  You can use this in conjunction with \n[jQuery.Model.static.convert] to provide automatic \n[jquery.model.typeconversion type conversion].  </p>\n\n<p>The following converts dueDates to JavaScript dates:</p>\n\n<pre><code class='javascript'>$.Model(\"Contact\",{\n  attributes : { \n    birthday : 'date'\n  },\n  convert : {\n    date : function(raw){\n      if(typeof raw == 'string'){\n        var matches = raw.match(/(\\d+)-(\\d+)-(\\d+)/)\n        return new Date( matches[1], \n                 (+matches[2])-1, \n                matches[3] )\n      }else if(raw instanceof Date){\n          return raw;\n      }\n    }\n  }\n},{})</code></pre>     ", "src": "../jquery/model/model.js", "children": []})