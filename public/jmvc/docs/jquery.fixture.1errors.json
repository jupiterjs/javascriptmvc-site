C({"name": "jquery.fixture.1errors", "type": "page", "comment": "<p>The following simulates an unauthorized request \nto <code>/foo</code>.</p>\n\n<pre><code>$.fixture(\"/foo\", function(){\n    return [401,\"{type: 'unauthorized'}\"]\n   });\n</code></pre>\n\n<p>This could be received by the following Ajax request:</p>\n\n<pre><code>$.ajax({\n  url: '/foo',\n  error : function(jqXhr, status, statusText){\n    // status === 'error'\n    // statusText === \"{type: 'unauthorized'}\"\n  }\n})\n</code></pre>", "title": "Simulating Errors", "parents": ["jQuery.fixture"], "src": "../jquery/dom/fixture/fixture.js", "children": ["getId"]})