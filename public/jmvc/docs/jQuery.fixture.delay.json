C({"name": "jQuery.fixture.delay", "type": "attribute", "parents": ["jQuery.fixture"], "comment": "<p>Sets the delay in milliseconds between an ajax request is made and\nthe success and complete handlers are called.  This only sets\nfunctional fixtures.  By default, the delay is 200ms.</p>\n\n<pre><code class='javascript'>steal('jquery/dom/fixtures').then(function(){\n  $.fixture.delay = 1000;\n})</code></pre> \n\n<p>Sets the delay in milliseconds between an ajax request is made and\nthe success and complete handlers are called.  This only sets\nfunctional fixtures.  By default, the delay is 200ms.</p>\n\n<pre><code class='javascript'>steal('jquery/dom/fixtures').then(function(){\n  $.fixture.delay = 1000;\n})</code></pre> ", "src": "../jquery/dom/fixture/fixture.js", "children": []})