C({"name": "jQuery.Model.jQuery.Model.prototype.backup", "params": {}, "ret": {"type": "undefined", "description": ""}, "type": "function", "parents": ["jquery.model.backup", "jQuery.Model"], "comment": "<p>Backs up an instance of a model, so it can be restored later.\nThe plugin also adds an [jQuery.Model.prototype.isDirty isDirty]\nmethod for checking if it is dirty.</p>\n\n<p>Backs up an instance of a model, so it can be restored later.\nThe plugin also adds an [jQuery.Model.prototype.isDirty isDirty]\nmethod for checking if it is dirty.</p>", "plugin": "jquery/model/backup", "src": "../jquery/model/backup/backup.js", "children": []})