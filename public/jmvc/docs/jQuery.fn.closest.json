C({"name": "jQuery.fn.closest", "type": "function", "parents": ["dom", "jQuery.fn"], "comment": "<p>Overwrites closest to allow open > selectors.  This allows controller actions such as:</p>\n\n<pre><code class='javascript'>\">li click\" : function( el, ev ) { ... }</code></pre> \n\n<p>Overwrites closest to allow open > selectors.  This allows controller actions such as:</p>\n\n<pre><code class='javascript'>\">li click\" : function( el, ev ) { ... }</code></pre> ", "src": "../jquery/dom/closest/closest.js", "children": []})